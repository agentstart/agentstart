/* agent-frontmatter:start
AGENT: Memory adapter re-export
PURPOSE: Re-exports in-memory adapter from @agentstart/memory
USAGE: import { inMemoryAdapter } from "agentstart/memory/in-memory"
EXPORTS: inMemoryAdapter
FEATURES:
  - Granular import to avoid bundling unused adapters
  - Lightweight in-memory implementation
SEARCHABLE: memory, in-memory, adapter, re-export
agent-frontmatter:end */

export * from "@agentstart/memory/in-memory";
