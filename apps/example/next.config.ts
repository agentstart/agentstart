/* agent-frontmatter:start
AGENT: Playground Next config
PURPOSE: Exposes the Next.js configuration object for the sample app.
USAGE: Imported by Next.js during build and runtime.
EXPORTS: default
FEATURES:
  - Keeps configuration typed via NextConfig
  - Provides a placeholder for additional Next.js options
SEARCHABLE: playground, next, config
agent-frontmatter:end */

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
