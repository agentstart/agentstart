/* agent-frontmatter:start
AGENT: Root TypeScript configuration
PURPOSE: Base TypeScript config for monorepo root
USAGE: Extended by packages and apps
FEATURES:
  - Path aliases for @agentstart/* packages
SEARCHABLE: typescript config, tsconfig, type configuration
agent-frontmatter:end */

{
  "extends": "@agentstart/tsconfig/internal-package.json",
  "compilerOptions": {
    "module": "esnext",
    "forceConsistentCasingInFileNames": true,
    "moduleResolution": "node"
  },
  "exclude": ["node_modules"],
  "include": ["scripts"]
}
